package ders15;
import java.time.LocalTime;
import java.time.LocalDate;
import java.time.LocalDateTime;

public class evisi2 {
    public static void main(String[] args){
        //Compare two `LocalDate` objects to determine which date comes first.
      /*  LocalDate date1 = LocalDate.of(2024,8,13);
        LocalDate date2 = LocalDate.of(2024,3,14);

        if (date1.isBefore(date2)){
            System.out.println( date1 + " birinci gelir" + date2 + "den");
        } else if (date1.isAfter(date2)){
            System.out.println(date2 + " birinci gelir" + date1+ "den");
        } else {
            System.out.println( date1 + " = " + date2);
        }*/

        //Convert a `LocalDateTime` object to an instance of `java.util.Date`.



    }
}
